arr = np.array([[8,2,4,6],[1,23,5,3]])
print("🌠원본 arr 행렬🌠:\n", arr)
#전체 행렬을 오름차순으로 정렬
print("전체를 오름차순 정렬:\n", np.sort(arr))
#각 열을 기준으로 내림차순으로 정렬
print("각 열 값을 내림차순 정렬:\n", np.sort(arr, axis=0)[::-1])
#정렬된 배열의 원래 인덱스를 반환
print("정렬된 배열의 인덱스값:\n", np.argsort(arr))
#sort 함수를 사용하면 원본이 유지됩니다.
print("sort 함수를 사용해 원본 유지:\n", arr)


#.sort() 메서드를 사용하면 원본이 정렬됨
arr.sort()
print("바뀐 arr 원본:\n", arr)
